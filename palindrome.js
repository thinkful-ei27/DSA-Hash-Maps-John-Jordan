'use strict';

const HashMap = require('./hashMap');
const palindromeHash = new HashMap();

// Is it possible to re-arrange these letters to make a palindrome?
console.log(palindromeHash._hashString('acecarr'));

// console.log(palindromeHash);